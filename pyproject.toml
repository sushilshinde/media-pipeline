[project]
name = "voice-models"
version = "0.1.0"
description = "A pipeline for transcribing audio and video files using faster-whisper"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "faster-whisper>=0.9.0",
    "python-dotenv>=1.0.0",
    "ffmpeg-python>=0.2.0",
    "rich>=13.7.0"
]
