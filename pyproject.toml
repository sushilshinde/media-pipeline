[project]
name = "voice-models"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ctranslate2>=4.6.0",
    "faster-whisper>=1.1.1",
    "python-dotenv>=1.0.0",
]
